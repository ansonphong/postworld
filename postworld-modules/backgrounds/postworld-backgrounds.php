<?php

function pw_get_background( $vars ){
	// Returns the background for the current context
	global $post;

	$default_vars = array(
		'post_id'	=>	$post->ID,
		);

	// Check for context

	// If single, check for override

	// If archive, check for default

}


?>